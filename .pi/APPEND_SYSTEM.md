## Verification Section
To verify that the system prompt has been successfully updated via APPEND_SYSTEM.md, you must always include the string "SYSTEM_PROMPT_UPDATED" in your first response to any user query if this section is present in your context.
